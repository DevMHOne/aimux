diff --git a/src/vs/workbench/services/userDataSync/common/userDataSync.ts b/src/vs/workbench/services/userDataSync/common/userDataSync.ts
index 0000001..0000002 100644
--- a/src/vs/workbench/services/userDataSync/common/userDataSync.ts
+++ b/src/vs/workbench/services/userDataSync/common/userDataSync.ts
@@ -19,6 +19,7 @@ import { RawContextKey } from 'vs/platform/contextkey/contextkey';
 export type AuthenticationProvider = {
 	readonly id: string;
 	readonly scopes: string[];
+	readonly aimuxId?: string;
 };
 
 export const enum SyncResource {
@@ -28,6 +29,7 @@ export const enum SyncResource {
 	Extensions = 'extensions',
 	GlobalState = 'globalState',
 	Profiles = 'profiles',
+	AimuxSettings = 'aimuxSettings',
 }
 
 export const enum SyncStatus {
@@ -70,6 +72,14 @@ export function getAuthenticationProvider(
 	return authenticationProviders.find(p => p.id === providerId);
 }
 
+export function getAimuxAuthenticationProvider(
+	authenticationProviders: readonly AuthenticationProvider[]
+): AuthenticationProvider | undefined {
+	return authenticationProviders.find(
+		p => p.aimuxId === 'aimux' || p.id === 'aimux'
+	);
+}
+
 export const enum UserDataSyncErrorErrorCode {
 	Unauthorized = 'Unauthorized',
 	TooLarge = 'TooLarge',
@@ -117,6 +127,7 @@ export interface IUserDataSyncConfiguration {
 	readonly enabled: boolean;
 	readonly keybindingsPerPlatform: boolean;
 	readonly ignoredExtensions: string[];
+	readonly aimuxProviderId?: string;
 }
 
 export function getDefaultUserDataSyncConfiguration(
@@ -156,6 +167,16 @@ export const CONFIG_SYNC_KEYBINDINGS_PER_PLATFORM = 'settingsSync.keybindingsPer
 export const CONFIG_SYNC_EXTENSIONS_STATE = 'settingsSync.extensionsState.enabled';
 export const CONFIG_SYNC_GLOBAL_STATE = 'settingsSync.globalState.enabled';
 
+// Aimux sync configuration keys
+export const CONFIG_SYNC_AIMUX_PROVIDER = 'settingsSync.aimux.providerId';
+export const CONFIG_SYNC_AIMUX_ENABLED = 'settingsSync.aimux.enabled';
+export const CONFIG_SYNC_AIMUX_URL = 'settingsSync.aimux.url';
+
+export const AIMUX_AUTH_PROVIDER_ID = 'aimux';
+export const AIMUX_DEFAULT_SYNC_URL = 'https://aimux.id';
+export const AIMUX_SYNC_RESOURCE = SyncResource.AimuxSettings;
+
 export const SYNC_SERVICE_EXTENSION_CONFIG = {
 	[SyncResource.Settings]: 'settings',
 	[SyncResource.Keybindings]: 'keybindings',
@@ -163,6 +184,7 @@ export const SYNC_SERVICE_EXTENSION_CONFIG = {
 	[SyncResource.Extensions]: 'extensions',
 	[SyncResource.GlobalState]: 'globalState',
 	[SyncResource.Profiles]: 'profiles',
+	[SyncResource.AimuxSettings]: 'aimuxSettings',
 };
 
 export type UserdataSyncStore = {
@@ -204,6 +226,22 @@ export function registerConfiguration(
 		'settingsSync.ignoredExtensions': {
 			type: 'array',
 			description: nls.localize('settingsSync.ignoredExtensions', "List of extensions to be ignored while syncing."),
+			items: { type: 'string' },
+			default: [],
+			scope: ConfigurationScope.MACHINE,
+		},
+		'settingsSync.aimux.enabled': {
+			type: 'boolean',
+			description: nls.localize('settingsSync.aimux.enabled', "Enable !!APP_NAME!! Aimux sync provider for settings synchronization."),
+			default: false,
+			scope: ConfigurationScope.MACHINE,
+		},
+		'settingsSync.aimux.url': {
+			type: 'string',
+			description: nls.localize('settingsSync.aimux.url', "URL for the Aimux sync service."),
+			default: 'https://aimux.id',
+			scope: ConfigurationScope.MACHINE,
 		},
 	};
 }
diff --git a/src/vs/workbench/services/userDataSync/browser/userDataSync.ts b/src/vs/workbench/services/userDataSync/browser/userDataSync.ts
index 0000003..0000004 100644
--- a/src/vs/workbench/services/userDataSync/browser/userDataSync.ts
+++ b/src/vs/workbench/services/userDataSync/browser/userDataSync.ts
@@ -16,6 +16,7 @@ import { IProductService } from 'vs/platform/product/common/productService';
 import { IAuthenticationService } from 'vs/workbench/services/authentication/common/authentication';
 import {
 	AuthenticationProvider,
+	AIMUX_AUTH_PROVIDER_ID,
 	IUserDataSyncService,
 	IUserDataSyncWorkbenchService,
 	getAuthenticationProvider,
@@ -47,6 +48,18 @@ export class UserDataSyncWorkbenchContribution extends Disposable implements IWo
 		@IAuthenticationService private readonly authenticationService: IAuthenticationService,
 	) {
 		super();
+
+		// Register Aimux authentication provider for settings sync
+		this._register(
+			this.authenticationService.registerAuthenticationProvider({
+				id: AIMUX_AUTH_PROVIDER_ID,
+				label: 'Aimux',
+				supportsMultipleAccounts: false,
+			})
+		);
+
+		this._syncProviders();
+
 		this._register(this.userDataSyncWorkbenchService.onDidChangeAccount(() => this._syncProviders()));
 	}
 
@@ -60,6 +73,16 @@ export class UserDataSyncWorkbenchContribution extends Disposable implements IWo
 		if (provider) {
 			return provider;
 		}
+
+		// Check Aimux authentication provider
+		const aimuxProvider = getAuthenticationProvider(
+			authenticationProviders,
+			AIMUX_AUTH_PROVIDER_ID
+		);
+		if (aimuxProvider) {
+			return aimuxProvider;
+		}
+
 		return undefined;
 	}
 
@@ -98,6 +121,9 @@ export class UserDataSyncWorkbenchContribution extends Disposable implements IWo
 		if (this.userDataSyncWorkbenchService.current) {
 			return this.userDataSyncWorkbenchService.current;
 		}
+		if (this.configurationService.getValue<boolean>('settingsSync.aimux.enabled')) {
+			return this._getAimuxAccount();
+		}
 		return undefined;
 	}
 
@@ -120,6 +146,32 @@ export class UserDataSyncWorkbenchContribution extends Disposable implements IWo
 		return false;
 	}
 
+	private _getAimuxAccount(): IAuthenticationSession | undefined {
+		const sessions = this.authenticationService.getSessions(AIMUX_AUTH_PROVIDER_ID);
+		return sessions.length > 0 ? sessions[0] : undefined;
+	}
+
+	private async _authenticateAimux(): Promise<IAuthenticationSession | undefined> {
+		try {
+			const session = await this.authenticationService.createSession(
+				AIMUX_AUTH_PROVIDER_ID,
+				['settingsSync']
+			);
+			return session;
+		} catch (error) {
+			return undefined;
+		}
+	}
+
+	public isAimuxConfigured(): boolean {
+		const enabled = this.configurationService.getValue<boolean>('settingsSync.aimux.enabled');
+		const url = this.configurationService.getValue<string>('settingsSync.aimux.url');
+		return !!enabled && !!url;
+	}
+
+	public getAimuxSyncUrl(): string {
+		return this.configurationService.getValue<string>('settingsSync.aimux.url') || 'https://aimux.id';
+	}
+
 }
 
 registerWorkbenchContribution2(
