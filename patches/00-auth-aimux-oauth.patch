diff --git a/src/vs/workbench/services/authentication/browser/authenticationService.ts b/src/vs/workbench/services/authentication/browser/authenticationService.ts
index 0000001..0000002 100644
--- a/src/vs/workbench/services/authentication/browser/authenticationService.ts
+++ b/src/vs/workbench/services/authentication/browser/authenticationService.ts
@@ -134,8 +134,21 @@ export class AuthenticationService extends Disposable implements IAuthentication
 		}));
 
+		// Register !!APP_NAME!! Aimux authentication provider
+		this._registerAimuxAuthenticationProvider();
+
 		this._registerEnvContributedAuthenticationProviders();
 		this._registerAuthenticationExtensionPointHandler();
 	}
 
+	private _registerAimuxAuthenticationProvider(): void {
+		const aimuxProvider: AuthenticationProviderInformation = {
+			id: 'aimux',
+			label: '!!APP_NAME!!',
+		};
+		this.registerDeclaredAuthenticationProvider(aimuxProvider);
+		this._logService.debug(`Registered !!APP_NAME!! Aimux authentication provider: ${aimuxProvider.id}`);
+	}
+
 	private _declaredProviders: AuthenticationProviderInformation[] = [];
 	get declaredProviders(): AuthenticationProviderInformation[] {
 		return this._declaredProviders;
